[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "qmap"
version = "0.1.0"
description = "Open-source QMAP benchmark suite"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]