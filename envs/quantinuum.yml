name: qmap_quantinuum
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - scipy
  - matplotlib
  - seaborn
  - pyyaml
  - python-dotenv
  - pip:
      - qnexus
      - pytket-qiskit
      - pytest
      # - -e .