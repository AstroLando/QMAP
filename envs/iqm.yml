name: qmap_iqm
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pip
  - pandas
  - python-dotenv
  - pip:
      - "iqm-client[qiskit]>=33.0.5"
      - "qiskit>=2.0.0,<2.2.0"
      - qiskit-ibm-runtime
      - pytest
      # - -e .